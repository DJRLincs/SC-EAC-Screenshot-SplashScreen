"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.PromptChannelNotFound = void 0;
const inquirer_1 = __importDefault(require("inquirer"));
class PromptChannelNotFound extends Error {
    constructor(message = 'Unknown Error') {
        super(message);
        Object.setPrototypeOf(this, PromptChannelNotFound.prototype);
    }
}
exports.PromptChannelNotFound = PromptChannelNotFound;
const selectChannel = (library, game) => {
    const games = Object.values(library);
    const channels = games.find((g) => g.id === game)?.channels;
    if (!channels || channels.length === 0) {
        throw new PromptChannelNotFound(`No channels found for game ${game}.`);
    }
    return [
        {
            type: 'list',
            name: 'channelID',
            message: 'Select a Channel:',
            choices: channels.map((channel) => ({
                name: channel.name,
                value: channel.id,
            })),
        },
    ];
};
const promptForChannel = async (library, game) => {
    const questions = selectChannel(library, game);
    return inquirer_1.default.prompt(questions);
};
exports.default = promptForChannel;
//# sourceMappingURL=channels.js.map