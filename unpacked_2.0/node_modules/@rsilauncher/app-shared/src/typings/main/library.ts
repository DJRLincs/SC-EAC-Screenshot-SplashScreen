export interface GameChannelData {
  platformId: string;
  gameId: string;
  gameName: string;
  channelId: string;
  channelName: string;
}

export enum GameChannelStatus {
  AVAILABLE = 'available',
  INSTALLED = 'installed',
  RUNNING = 'running',
  UPDATE_AVAILABLE = 'update-available',
}
export interface WatcherSelectGame {
  libraryFolder: string;
  gameId: string;
  channelId: string;
  installDir?: string;
}
